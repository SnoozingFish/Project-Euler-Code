def gcd(a, b):
    while b:      
        a, b = b, a % b
    return a

def lcm(a, b):
    return a * b // gcd(a, b)
    
current = 20
guess = 20    

while current > 0:
  guess = lcm(guess, current)
  current -= 1
print(str(guess))
