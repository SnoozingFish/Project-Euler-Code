import time
import math

alpha = 999
beta = 999
digits = 3
#limiter = 100
bestGuess = 0

while len(str(alpha)) == digits:
  guess = alpha * beta
  
  if str(guess) == str(guess)[::-1]:
    print(str(alpha) + " * " + str(beta) + " = " + str(alpha * beta))
    
    if guess > bestGuess:
      bestGuess, bestAlpha, bestBeta = guess, alpha, beta
      #limiter = round(math.sqrt(bestGuess))
      
  
  if alpha == limiter and beta == limiter:
    print("")
    print(str(bestGuess) + ", " + str(bestAlpha) + " * " + str(bestBeta))
    break
  
  elif beta == limiter:  
    alpha -= 1
    beta = alpha
  else:
    beta -= 1 
