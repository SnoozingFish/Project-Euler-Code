import time

alpha = 999
beta = 999
digits = 3
bestGuess = 0

while len(str(alpha)) == digits:
  guess = alpha * beta
  
  if str(guess) == str(guess)[::-1]:
    print(str(alpha) + " * " + str(beta) + " = " + str(alpha * beta))
    if guess > bestGuess:
      bestGuess, bestAlpha, bestBeta = guess, alpha, beta
  
  if alpha == 100 and beta == 100:
    print("")
    print(str(bestGuess) + ", " + str(bestAlpha) + " * " + str(bestBeta))
    
    end = time.time()
    print(str(end - start) + " seconds.")
    break
  
  elif beta == 100:  
    alpha -= 1
    beta = alpha
  else:
    beta -= 1 

